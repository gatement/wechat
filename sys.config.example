[
    %{kernel, [
    %    {error_logger, {file, "/var/log/gizwits/m2m_core/kernel.log"}},
    %    {inet_dist_listen_min, 20000}, % limit distributed erlang listening port
    %    {inet_dist_listen_max, 20000}
    %]},
    %{sasl, [
    %    {sasl_error_logger, {file, "/var/log/gizwits/m2m_core/sasl.log"}},
    %    {errlog_type, error}
    %]},
    {wechat, [
        {http_port, 8080}
    ]}
].
